# Задачи

Единственный список плановых фич, улучшений, порядка работы и статусов для Tilebreaker.

**Как выбирать работу:** брать первый незачеркнутый заголовок сверху вниз. Если задача слишком крупная, внутри нее можно вести короткий checklist, но отдельный порядок задач и acceptance все равно живут только в этом файле.

`todo/current.md` не содержит задач, next-step, acceptance или статусов. Это только снимок текущей версии и дизайн-контекст.

---

### ~~[2026-05-08] Определить core-loop Tilebreaker~~ ВЫПОЛНЕНО

**Статус:** core-loop переопределен в `design/core.md`.

---

### ~~[2026-05-08] Проверить реализуемость стартовых тайлов симуляцией~~ ВЫПОЛНЕНО

**Статус:** добавлен `scripts/simulate-tiles.js`, результаты и выводы записаны в `design/tile-feasibility.md`.

---

### ~~[2026-05-08] День 1: заменить заглушку боя на playable tile-placement MVP~~ ВЫПОЛНЕНО

**Статус:** заглушка боя заменена на playable tile-placement round-loop: поле 6x6, рука 7 тайлов из `assets/tiles_v2/tile_manifest.json`, строгие совпадения краев, capture-fill через micro-grid, урон `area * 2`, HP игрока/врага, цветовые атаки, результат раунда и обновление поля/руки. Smoke-тест проходит весь 5-бойный цикл через реальные размещения тайлов.

---

### ~~[2026-05-08] День 1: пересобрать стартовую колоду под capture-fill~~ ВЫПОЛНЕНО

**Статус:** v2-набор лежит в `assets/tiles_v2`; `dot/cap` убраны из MVP-колоды; симуляция берет `assets/tiles_v2/tile_manifest.json`; первый бой в строгой модели: 69/100 побед. Подробности: `design/tile-feasibility.md`.

---

### ~~[2026-05-08] День 1: сгладить волатильность v2 capture-fill~~ ВЫПОЛНЕНО

**Статус:** `scripts/simulate-tiles.js` переключен на `damage = area * 2` и сглаживание руки через лучшую из 3 кандидатных раздач. В строгой модели нулевые сглаженные руки снизились с 94/100 до 75/100, максимальный удар упал со 144 до 24, первый бой стал 79/100 побед без ваншота. Подробности: `design/tile-feasibility.md`.

---

### ~~[2026-05-08] День 1: вынести параметры tile-battle в конфиги~~ ВЫПОЛНЕНО

**Статус:** параметры v2 tile-battle живут в JSON: поле, рука, HP игрока, размер стартовой колоды, формула урона и manifest path в `configs/game.json`; HP врагов и цветовые атаки по раундам в `configs/levels.json`; старые `targetScore` удалены. Smoke-тест проходит 5-бойный цикл.

---

### ~~[2026-05-08] День 1: сделать читаемый combat UI~~ ВЫПОЛНЕНО

**Статус:** combat UI показывает HP игрока/врага, номер раунда, цветовые атаки, поле, руку и итог раунда по каждому цвету: атаку, площадь захвата, сумму захвата, урон врагу или урон игроку. Smoke-тест проверяет наличие debug-данных для результата по цветам.

---

### ~~[2026-05-08] День 2: добавить колоду, сброс и простую прогрессию между боями~~ ВЫПОЛНЕНО

**Статус:** стартовая колода строится из 36 тайлов v2 manifest; руки добираются из draw pile через best-of-3 smoothing, сыгранные и оставшиеся тайлы уходят в discard, пустой draw pile перемешивается из discard. После победы игрок выбирает одну из трех наград: добавить тайл, удалить тайл или усилить цветовой множитель. Smoke-тест проходит полный 5-бойный цикл с реальными наградами.

---

### ~~[2026-05-08] День 2: наиграть и поправить базовый баланс~~ ВЫПОЛНЕНО

**Статус:** принята persistent board модель: между раундами очищаются только закрытые/засчитанные тайлы, незакрытая территория остается для достраивания, а полный dead-end восстанавливается через fresh start. Симулятор читает актуальные JSON-конфиги, собирает win rate/rounds/player damage/dead-end rounds и фиксирует баланс-пасс в `design/tile-feasibility.md`. Smoke-тест проходит полный 5-бойный цикл.

---

### [2026-05-08] День 3: убрать автосборку красного квадрата и вернуть ощущение честного прогона

**Idea:** разрезать переуправляемую раздачу, из-за которой каждый run ощущается одинаково и почти всегда толкает игрока в красный квадрат.

**Why:** сейчас проблема не в polish, а в том, что базовый ход скучный: игрок видит один и тот же сценарий, другие цвета почти не собираются, а генератор играет за него.

**MVP:**

- убрать или выключить `guaranteedLoopHands` как дефолтный режим;
- убрать автодостройку полного loop из draw pile для обычного боя;
- заменить фиксированный seed на новый seed для каждого run или явно показывать seed и кнопку/способ начать новый;
- исправить bias `selectPrimaryAttackColor`, где при равных атаках красный становится дефолтом;
- оставить deterministic seed только для smoke-теста или debug-режима;
- записать в `design/tile-feasibility.md`, стало ли меньше повторяемости и собираются ли синий/зеленый.

**Acceptance:** игрок запускает несколько новых run и видит разный порядок тайлов/цветов; красный квадрат больше не является системным первым решением; smoke-тест остается стабильным через debug seed.

**Priority:** must

**Layer:** MVP

---

### [2026-05-08] День 3: попробовать queue-режим "текущий тайл + следующий"

**Idea:** вместо руки из 7 тайлов выдать игроку один текущий тайл и preview следующего.

**Why:** это может сделать каждый ход интереснее: игрок решает, куда поставить текущий тайл, зная один будущий кусок, а не ищет готовый квадрат в полной руке.

**MVP:**

- добавить config-переключатель `drawMode`: `hand` или `queue`;
- в `queue` показывать текущий тайл крупнее и следующий тайл как preview;
- ставить можно только текущий тайл;
- после постановки preview становится текущим, добирается новый preview из draw pile;
- конец раунда после фиксированного числа сыгранных тайлов или по кнопке;
- `discard` получает сыгранные тайлы и пропущенные/оставшиеся queue-тайлы по понятному правилу;
- smoke-тест должен проходить основной цикл в активном draw mode.

**Acceptance:** игрок видит текущий тайл и следующий, ставит текущий, preview сдвигается вперед; за один бой появляются решения "куда поставить сейчас с учетом следующего".

**Priority:** must

**Layer:** MVP

---

### [2026-05-08] День 3: попробовать честный color/pattern bag

**Idea:** заменить чистый shuffle и best-of на bag-выдачу, где за короткое окно проходят разные цвета и формы.

**Why:** нужно убрать длинные серии одного цвета/паттерна без ручной автосборки loops.

**MVP:**

- сделать bag, который перемешивает v2-тайлы пачками с контролем цвета: red/blue/green должны регулярно появляться в окне 8-12 выдач;
- серые тайлы дозировать отдельно, чтобы они создавали напряжение, но не забивали early-run;
- проверить bag в `hand` и/или `queue` режиме;
- симулятор должен печатать распределение цветов/паттернов по первым N выдачам.

**Acceptance:** в первых 8-12 выдачах игрок видит несколько цветов, а не повтор одного красного сценария; при этом нет гарантированной готовой петли "из коробки".

**Priority:** must

**Layer:** MVP

---

### [2026-05-08] День 3: сравнить hand vs queue и выбрать MVP-режим

**Idea:** быстро наиграть два режима выдачи и оставить тот, где ход чаще дает интересный выбор.

**Why:** нельзя полировать две разные игры; нужен один основной режим для playable build.

**MVP:**

- пройти хотя бы по 2-3 коротких run в `hand` и `queue`;
- для каждого режима записать ощущения: есть ли выбор, понятен ли риск/награда, собираются ли разные цвета, насколько часто игрок просто ждет нужный угол;
- сравнить симуляторные метрики: win rate, средний урон игрока, dead-end rounds, распределение цветов, число захватов;
- зафиксировать решение в `design/decisions.md` и `todo/current.md`.

**Acceptance:** выбран один активный `drawMode` для MVP; альтернативный режим либо удален, либо оставлен как выключенный эксперимент в конфиге.

**Priority:** must

**Layer:** MVP

---

### [2026-05-08] День 3: polish, juice и сборка playable build

**Idea:** довести прототип до состояния, где его можно дать поиграть без объяснений в чате.

**Why:** к концу 40-часового окна нужен playable build, а не только механика.

**MVP:**

- подсветка валидных клеток;
- подсветка захваченных областей;
- цветные числа урона;
- понятные кнопки конца раунда и перехода дальше;
- smoke-тест на запуск и прохождение основного цикла;
- обновить README с актуальным описанием.

**Acceptance:** игрок открывает билд, проходит меню -> бой -> результат -> улучшение -> следующий бой, и понимает главную механику без внешнего объяснения.

**Priority:** must

**Layer:** MVP

---

### [2026-05-08] Stretch: артефакты и слом правил

**Idea:** добавить первые эффекты, которые дают Balatro-ощущение слома правил.

**Why:** это усилит долгосрочную глубину, но только после того, как базовая выкладка заработает.

**MVP:**

- 3-5 простых артефактов;
- эффекты только на множители или подсчет зон;
- без сложных уникальных врагов.

**Acceptance:** игрок после победы может получить артефакт, и следующий бой заметно меняет расчет урона.

**Priority:** nice

**Layer:** Jam Stretch
