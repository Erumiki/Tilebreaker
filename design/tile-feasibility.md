# Tile Feasibility Test

Дата: 2026-05-08.

Цель: до реализации боевой сцены проверить, складывается ли идея tile-placement боя в принципе: можно ли придумать стартовую колоду, случайно раздать руки, замкнуть цветные границы, захватить землю, нанести урон и теоретически проходить уровни.

## Модель теста

Скрипт: `scripts/simulate-tiles.js`.

Параметры:

- поле 6x6;
- рука 7 тайлов;
- активная стартовая колода v3-эксперимента: 25 тайлов из `startingDeckRecipe`;
- честный draw без сглаживания руки; debug-сглаживание остается только для smoke/debug;
- opening `drawBag` может переставлять первые 12 будущих доборов боя без добавления тайлов;
- 100 симуляций каждого теоретического боя;
- 250 случайных попыток сборки на каждую кандидатную руку;
- урон захвата = площадь * 2;
- в бою невыбранные кандидатные руки уходят в сброс, выбранная рука играется как обычная;
- если цвет перебил атаку, во врага идет вся закрытая сумма цвета;
- если цвет не перебил атаку, игрок получает недостачу.

Временная модель тайла для теста: каждый тайл — сетка 3x3 микроземель. Края тайлов матчятся по 3-символьной подписи. Актуальная версия симуляции считает не закрытые цветные компоненты, а захват площади цветной границей.

Это не финальный формат тайлов, но хорошая проверка топологии: есть ли замыкаемые зоны, можно ли их растить, насколько часто рука мертвая.

## День 3: tiles v3 two-color experiment

После сравнения `hand` и `queue` стало ясно, что главная проблема не только в режиме выдачи: три боевых цвета дробят план, а фактические закрытия почти всегда остаются минимальными `area 12`. Rotation не берем как базовый фикс, потому что он слишком усиливает углы и удешевляет ориентацию.

Активный v3-эксперимент без нового арта:

- `activeCombatColors = ["red", "blue"]`;
- green остается в manifest и UI, но не входит в стартовую колоду, early rewards и первые две таблицы атак;
- стартовая колода 25 тайлов: на red/blue `line_h x2`, `line_v x2`, каждый `tee` x1, каждый `corner` x1, `plus x0`, плюс `tile_gray_blank_01 x1`;
- opening bag: `corner <= 2`, `plus <= 0`, `line >= 4`, `tee >= 4`, red/blue минимум по 4, `grayMax = 1`;
- активный режим остается `queue`; `hand` доступен через `?drawMode=hand` для сравнения.

Короткий прогон активного `queue + opening bag`:

```sh
HAND_RUNS=40 FIGHT_RUNS=40 QUEUE_BEAM_WIDTH=16 ./scripts/node.sh scripts/simulate-tiles.js 20260508
```

Ключевые результаты для battle_01:

```text
wins 21/40 (53%)
captures 44
minimal captures 28/44 = 64%
avg capture area 17.1
zero streak avg 4.8, p75 7
captures in 3r 17/40
dead-end 28/254 rounds
```

Для сравнения, до v3 активный queue/opening часто держался около `98%` малых закрытий и `avg area` около 12. Вывод: two-color v3 заметно улучшает размер зон и снижает доминирование минимального квадрата, но zero streak все еще длинный; следующий рычаг лучше искать в контроле очереди или ценности setup-раундов, а не в базовом rotation.

## День 4: gameplay variants scaffold

Добавлен общий переключатель `gameplayVariant` для сравнения legacy и будущих вариантов A-D без форка кода. Текущий `queue + two-color capture-fill` сохранен как `legacy`, а старый `baseline` остается алиасом. На этом шаге все не-legacy варианты используют legacy-правила, но уже запускаются через config/URL, выбираются на стартовом экране, видны в UI/debug и печатаются в симуляторе.

Порядок сравнения и ручной scorecard вынесены в `design/gameplay-variants.md`. Симуляционные метрики остаются теми же: win rate, `minimal capture share`, `avg capture area`, `placements before capture`, zero-damage streak, captures in 3 rounds и dead-end rounds.

## День 3: queue draw experiment

Добавлен режим `tileBattle.drawMode`:

- `queue` — активный MVP-режим: игрок видит текущий тайл и preview следующего, ставить можно только текущий;
- `hand` — режим сравнения: игрок видит полную руку.

Hand можно открыть через `?drawMode=hand`, а queue-симулятор прогнать через:

```sh
DRAW_MODE=queue ./scripts/node.sh scripts/simulate-tiles.js 20260508
```

Для queue-режима симулятор использует beam AI, а не случайную перестановку руки. Это важное отличие: при текущем тайле и одном preview вариантов меньше, поэтому тестовый игрок может смотреть все валидные клетки текущего тайла и держать несколько лучших веток.

Короткая проверка `DRAW_MODE=queue HAND_RUNS=8 FIGHT_RUNS=8 QUEUE_BEAM_WIDTH=16`:

- одиночные queue-окна чаще всего ставят 7/7 тайлов;
- для `current deck / opening bag`: 1/8 sampled queue windows дал захват, `avg damage 3.0`, `minimal capture share 100%`;
- `battle_01 / current deck / opening bag`: 5/8 побед, zero streak `avg 3.6`, captures in 3r `5/8`;
- beam AI заметно лучше прежней greedy-оценки и подтверждает, что queue действительно проще тестировать программно;
- когда захваты появляются, они все еще в основном малые `area <= 12`;
- active MVP переключен на `queue`, чтобы обычный playable build проверял именно текущий тайл + preview.

Вывод: queue реализован, измеряется и включен по умолчанию для ручного feel. Он снижает branching и делает автотест потенциально надежнее, но сам по себе не решает доминирование малого контура. Следующий шаг — сравнить hand recipe/bag vs queue на одинаковой силе AI и ручном feel, затем уже решить, оставлять ли queue финальным active `drawMode`.

## Обновление правила подсчета после арт-проверки

После первого пакета ассетов стало понятно, что center-exit тайлы визуально читаются как цветные границы, а не как готовая закрашенная земля. Для MVP принимаем более "земельное" правило:

```text
цветная граница замкнулась -> внутренняя область заливается этим цветом -> очки считаются по площади
```

Технически симуляция считает захват через flood fill:

1. Собрать все поставленные тайлы в общий `18x18` micro-grid для поля `6x6`.
2. Для каждого боевого цвета отдельно считать micro-cells этого цвета стеной.
3. Запустить flood fill по всем клеткам, которые не являются этим цветом, начиная с внешней рамки.
4. Все нецветные micro-cells, до которых flood fill не добрался, считаются захваченной внутренностью. Это включает и поставленные клетки, и пустую внутренность без тайлов.
5. Сила цвета считается по площади `граница + захваченная внутренность`.

Важные уточнения для симуляции:

- используется 4-соседство, диагонали не закрывают область;
- пустые macro-cells поля считаются внешним воздухом только если связаны с внешней областью;
- пустая область внутри полностью замкнутой границы становится захваченной землей и дает очки;
- поставленные клетки других боевых цветов внутри замкнутой границы считаются захваченной внутренностью для текущего цвета;
- красный, синий и зеленый считаются независимо;
- визуальная заливка внутренности не меняет edge signatures тайлов;
- старая статистика ниже полезна как проверка выкладываемости и базовой топологии, но баланс урона пересчитан под новое правило площади.

## Стартовая колода v2 для следующей проверки

На каждый боевой цвет — красный, синий, зеленый:

- `line_h` — горизонтальная линия;
- `line_v` — вертикальная линия;
- `corner_ur`, `corner_rd`, `corner_dl`, `corner_lu` — повороты.
- `tee_u`, `tee_r`, `tee_d`, `tee_l` — T-развилки;
- `plus` — перекресток границы, замена старого `dot`.

Плюс 3 серых пустых тайла.

Всего: 36 тайлов.

В v2 нет обычного `dot` с цветной клеткой только в центре: такой тайл не имеет выхода на край и не помогает строить границу. Базовые `cap`-тайлы тоже убираются из стартового набора, потому что они часто создают обрубки вместо замыканий. Их можно вернуть позже как special/low-power тайлы.

## День 3: payoff против минимального квадрата

Симуляция обновлена под честный draw, persistent board, fresh-start recovery и формулу из `configs/game.json`:

```text
damage = area * 2
largeZoneBonus = max(0, area - 12) * 2
grayInteriorBonus = gray tile micro-cells inside captured interior * 1
grayWildcardPlacement = true
```

Контрольный сценарий показывает, зачем теперь достраивать:

```text
minimal 2x2 loop:       area 12, area bonus 0, gray bonus 0, damage 24
3x3 loop with gray tile: area 41, area bonus 58, gray bonus 9, damage 149
```

То есть минимальный квадрат остается валидным безопасным ходом, но большая зона с серым fill становится отдельной ставкой, а не декоративным оверкиллом без понятного payoff.

Сводка прогона `HAND_RUNS=40 FIGHT_RUNS=40 PLACEMENT_ATTEMPTS=40`:

```text
current deck:      hand captures avg 0.1, zero damage 37/40, battle_01 wins 20/40, avg capture area 12.5
fewer corners:     hand captures avg 0.1, zero damage 37/40, battle_01 wins 17/40, avg capture area 13.3
fewer plus:        hand captures avg 0.1, zero damage 37/40, battle_01 wins 15/40, avg capture area 12.9
fewer both:        hand captures avg 0.0, zero damage 39/40, battle_01 wins 12/40, avg capture area 12.6
```

Выводы:

- Урезание `plus` и `corner` не берем в MVP-конфиг прямо сейчас: оно снижает частоту побед и повышает ожидание нужных кусков сильнее, чем растит среднюю площадь.
- Payoff-фикс сам по себе не заставляет случайного бота регулярно строить большие зоны; он делает такие зоны резко выгоднее, когда игрок или будущий режим выдачи дает возможность их планировать.
- Серый blank теперь считается neutral wildcard при размещении: его можно ставить рядом с цветной границей и достраивать цветную границу рядом с ним, чтобы `grayInteriorBonus` был достижимым правилом, а не только формулой.
- Следующие рычаги по плану — queue-режим и честный color/pattern bag: они должны чаще давать игроку промежуточный выбор, а не только редкую готовую петлю.

## День 3: opening draw bag

Симулятор и игра теперь читают `tileBattle.drawBag` из `configs/game.json`. Bag применяется один раз на старте боя и переставляет только ближайшее окно будущих доборов из текущего draw pile:

```text
openingDraws = 12
patternCaps = corner <= 3, plus <= 1
patternMinimums = line >= 3, tee >= 4
combatColorMinimums = red/blue/green >= 2
grayMax = 2
```

Контрольный быстрый прогон `HAND_RUNS=8 FIGHT_RUNS=8 PLACEMENT_ATTEMPTS=12` на seed `20260508` показал, что окно действительно получает продолжатели без 4-corner готовой петли:

```text
current recipe opening: green:plus | blue:tee_l | blue:line_h | green:tee_r | ...
shapes: corner:3, line:2, plus:1, tee:6

opening bag: green:corner_dl | red:tee_u | red:plus | blue:line_h | ...
shapes: corner:3, gray:1, line:3, plus:1, tee:4
```

Боевые метрики остались смешанными: на маленьком прогоне `battle_01` остался `2/8` побед в current deck, средняя зона в отдельных боях иногда растет до `13.3-14.0`, но zero-damage streak все еще может доходить до 8 раундов. Вывод: opening bag выполняет MVP-задачу по форме раннего окна, но не должен разрастаться в hidden solver. Если ручной feel все еще сухой, следующий эксперимент — queue-режим или отдельное ослабление малого контура.

## День 3: baseline доминирования минимального квадрата

Задача этого прогона — не менять правила, а измерить, насколько текущая выдача и эвристика все еще приводят к малому `area <= 12` закрытию.

Симулятор теперь печатает дополнительные диагностические метрики:

- `minimal capture share`: доля закрытых зон с `area <= 12`;
- `avg capture area`: средняя площадь закрытия;
- `placements before capture`: сколько поставленных тайлов проходит до первого закрытия в руке;
- `zero damage hands` и `zero ... rounds`;
- `hands with 4-corner loop`;
- первые 12 выдач: последовательность, цвета и shape-группы `corner/plus/line/tee/gray`;
- первые 12 hand-samples: счетчики цветов и shape-групп в раздаче.

Команда baseline:

```sh
HAND_RUNS=40 FIGHT_RUNS=40 PLACEMENT_ATTEMPTS=40 ./scripts/node.sh scripts/simulate-tiles.js 20260508
```

Baseline для `STRICT edge matching / current deck`, seed `20260508`:

```text
first 12 draws:
colors: blue 6, green 3, red 3
shapes: corner 4, line 4, plus 1, tee 3

40 honest hands:
captures avg 0.2
total damage avg 4.2
avg capture area 12.0
minimal capture share 7/7 = 100%
placements before capture avg 6.0
quick 4-corner loops 0/40
zero damage hands 33/40
all tiles placed 40/40

theoretical battles:
battle_01 wins 23/40, captures 52, minimal 48/52 = 92%, avg area 12.9, zero damage 199/250 rounds
battle_02 wins 20/40, captures 55, minimal 53/55 = 96%, avg area 12.6, zero damage 206/258 rounds
battle_03 wins  7/40, captures 56, minimal 55/56 = 98%, avg area 12.2, zero damage 256/309 rounds
battle_04 wins  8/40, captures 61, minimal 60/61 = 98%, avg area 12.4, zero damage 247/303 rounds
battle_05 wins  3/40, captures 59, minimal 57/59 = 97%, avg area 12.4, zero damage 256/312 rounds
```

Сравнение с быстрыми deck-предсетами подтверждает старый риск: простое удаление `corner`/`plus` не решает проблему, а в основном снижает проходимость. В hand-тестах все закрытия у `current`, `fewer corners`, `fewer plus` и `fewer both` снова имеют `avg capture area 12.0` и `minimal capture share 100%`.

Вывод: payoff-формула уже делает большие зоны выгодными в контролируемом сценарии, но текущая честная hand-выдача почти никогда не приводит к большим зонам сама. Следующая задача должна работать с составом стартовой колоды/recipe и потом с opening bag или queue, а не с уроном.

## День 3: anti-small-square starting deck recipe

Игра и симулятор теперь читают один источник стартовой колоды: `tileBattle.startingDeckRecipe` в `configs/game.json`. Recipe разворачивается в массив tile id и поддерживает дубли без изменения `assets/tiles_v2/tile_manifest.json`.

Принятый MVP-рецепт консервативный:

- все combat-тайлы v2 остаются по одному экземпляру;
- `tile_gray_blank_01` получает вторую копию;
- всего в стартовой колоде 37 tile ids;
- `add_tile` добавляет одну копию id, `remove_tile` удаляет одну копию id.

Проверенная более жесткая версия с 44 картами, дублированными `line_h/line_v` и 5 gray blank снижала готовые быстрые петли, но сильно сушила первый бой: `battle_01 wins 10/40` против baseline `23/40`. Поэтому ее не берем как активный MVP-рецепт до opening bag.

Команда:

```sh
HAND_RUNS=40 FIGHT_RUNS=40 ./scripts/node.sh scripts/simulate-tiles.js 20260508
```

Результаты для `STRICT edge matching / current deck (37 tiles)`:

```text
first 12 draws:
colors: blue 4, green 4, red 4
shapes: corner 3, line 2, plus 1, tee 6

40 honest hands:
captures avg 0.1
total damage avg 1.2
avg capture area 12.0
minimal capture share 2/2 = 100%
quick 4-corner loops 1/40
zero damage hands 38/40

theoretical battles:
battle_01 wins 14/40, captures 39, minimal 35/39 = 90%, avg area 13.7, zero damage 248/287 rounds
battle_02 wins 15/40, captures 44, minimal 43/44 = 98%, avg area 12.3, zero damage 235/278 rounds
battle_03 wins  3/40, captures 52, minimal 52/52 = 100%, avg area 12.0, zero damage 257/309 rounds
battle_04 wins  8/40, captures 50, minimal 46/50 = 92%, avg area 13.2, zero damage 249/297 rounds
battle_05 wins  0/40, captures 44, minimal 43/44 = 98%, avg area 12.3, zero damage 277/320 rounds
```

Вывод: recipe как изолированный рычаг дает небольшой сдвиг в сторону больших закрытий (`battle_01 avg area 13.7`, minimal share 90% вместо 92%), но не решает задачу полностью. Сильное урезание `corner/plus` ухудшает частоту урона и первый бой быстрее, чем улучшает размер зон. Следующая задача `opening shape-bag with cap on corner/plus` остается must: ограничивать раннюю выдачу замыкателей нужно по окну первых draw, а не только общим составом колоды.

Важное уточнение по интерпретации: `zero damage` в первом раунде не является провалом, если persistent board сохраняет полезный контур, который закрывается позже. Для следующих симуляций нужно смотреть не только `zero damage rounds/hands`, а окно 2-3 раундов: длину нулевых серий, закрытия после setup-раунда, `dead-end/freshStart`, win rate и player damage. Не оптимизировать выдачу под гарантированный удар в R1: это снова подтолкнет систему к hidden loop guarantee и малым квадратам.

## Результаты сглаженной v2 capture-fill

Симуляция использует `assets/tiles_v2/tile_manifest.json`, строгий edge matching как основной режим, `damage = area * 2` и сглаживание раздачи через выбор лучшей из 3 кандидатных рук.

### Строгое совпадение краев

100 сглаженных рук:

- в среднем ставится 7.0 из 7 тайлов;
- все 7 тайлов поставлены в 97 из 100 рук;
- среднее число захватов: 0.3;
- средний урон: 6.0;
- 75 из 100 рук не нанесли урона;
- максимальный урон руки: 24.

Теоретические бои без апгрейдов:

```text
battle_01: 79/100 побед
battle_02: 39/100 побед
battle_03: 17/100 побед
battle_04:  2/100 побед
battle_05:  0/100 побед
```

Вывод: для первого боя это лучше подходит под 3-минутный MVP. Ненулевые руки встречаются чаще 6%, максимальный удар больше не ваншотит стартового врага на 35 HP, а поздние бои без апгрейдов не становятся автопроходимыми.

### Свободная adjacency-модель

100 сглаженных рук:

- в среднем ставится 7.0 из 7 тайлов;
- все 7 тайлов поставлены в 100 из 100 рук;
- среднее число захватов: 0.1;
- средний урон: 2.9;
- 88 из 100 рук не нанесли урона;
- максимальный урон руки: 24.

Теоретические бои:

```text
battle_01: 28/100 побед
battle_02:  2/100 побед
battle_03:  1/100 побед
battle_04:  0/100 побед
battle_05:  0/100 побед
```

Вывод: свободная adjacency-модель по-прежнему хуже строгой для этой эвристики. Для MVP остаемся на строгом edge matching.

## Что это меняет

1. Базовая формула для первого прототипа: `damage = captured area * 2`, а не `area * area`.

2. Первый бой должен использовать сглаживание стартовой руки/редроу: выбрать лучшую из 3 кандидатных раздач, остальные отправить в сброс. Это дает игроку больше ранних маленьких захватов, не ломая идею колоды.

3. Для первого боя текущие теоретические значения остаются приемлемыми: `35 HP`, атаки `1-2`, стартовый игрок `45 HP`.

4. Поздние бои в этой проверке не обязаны быть честными без апгрейдов. Их баланс нужно возвращать после реализации наград/прогрессии.

## Баланс-пасс: persistent board

Гипотеза: если в начале следующего раунда убирать только закрытые/засчитанные тайлы, а незакрытые оставлять на поле, игрок сможет достраивать территорию несколько ходов. Ожидаемый эффект: меньше ощущения dead-end после первого тайла, больше средних захватов, больше урона игроку за недобор цветов, без возврата к автопобеде.

Решение для MVP:

- `roundBoardCleanup = clearScoredTiles`: после результата раунда с поля удаляются тайлы, чьи micro-cells участвовали в засчитанной зоне; незакрытые тайлы остаются.
- `deadEndRecovery = freshStart`: если новая рука вообще не может продолжить сохраненную доску, незакрытая доска очищается и игрок начинает новый остров этой рукой.
- симулятор теперь читает `configs/game.json` и `configs/levels.json`, по умолчанию гоняет строгий edge matching; loose baseline можно включить флагом `--with-loose`.

Проверка: seed `20260508`, strict edge matching, 40 сглаженных рук, 40 прогонов каждого боя, 40 случайных попыток выкладки на руку, урон `area * 2`, игрок `120 HP`, текущие HP/атаки из `configs/levels.json`.

```text
smoothed hands:
placed avg 7.0/7, captures avg 0.2, total damage avg 4.2, zero damage 33/40

battle_01: 34/40 побед, avg 4.3 раунда, avg player damage 15.3, dead-end 9/172 rounds
battle_02: 35/40 побед, avg 4.2 раунда, avg player damage 24.4, dead-end 9/167 rounds
battle_03: 25/40 побед, avg 6.2 раунда, avg player damage 53.6, dead-end 17/246 rounds
battle_04: 28/40 побед, avg 6.0 раунда, avg player damage 65.3, dead-end 19/238 rounds
battle_05: 16/40 побед, avg 7.0 раунда, avg player damage 97.7, dead-end 21/282 rounds
```

Вывод: гипотезу принимаем в MVP. Persistent board делает незакрытые куски полезными между раундами, а `freshStart` убирает жесткое зависание "есть рука, но продолжать невозможно". Первый бой щадящий, 2-3 требуют хотя бы одного нормального захвата, 4-5 уже едят много HP и должны опираться на награды. Баланс пока оставляем без изменения состава колоды: следующий риск не математика, а читаемость валидных ходов и сохраненной незакрытой территории.

### Уточнение: off-color leap для заблокированного тайла

Гипотеза: если выбранный боевой тайл не имеет прямого edge-match продолжения, разрешить поставить его через одну пустую клетку от тайла другого боевого цвета. Ожидаемый эффект: игрок сможет начать новый цветовой остров рядом с заблокированной доской, но прямое продолжение контура останется основным ходом.

Решение для MVP:

- `offColorLeapPlacement = true`;
- `offColorLeapDistance = 2`;
- `offColorLeapOnlyWhenBlocked = true`;
- серые тайлы не используют leap, чтобы пустышки не создавали бессмысленные острова.

Проверка: seed `20260508`, strict edge matching, 40 сглаженных рук, 40 прогонов каждого боя, 40 случайных попыток выкладки на руку.

```text
smoothed hands:
placed avg 7.0/7, captures avg 0.3, total damage avg 6.0, zero damage 30/40

battle_01: 31/40 побед, avg 5.9 раунда, avg player damage 22.3, dead-end 21/236 rounds
battle_02: 33/40 побед, avg 4.5 раунда, avg player damage 26.6, dead-end 11/180 rounds
battle_03: 23/40 побед, avg 5.8 раунда, avg player damage 51.9, dead-end 16/233 rounds
battle_04: 27/40 побед, avg 6.5 раунда, avg player damage 72.4, dead-end 21/258 rounds
battle_05: 15/40 побед, avg 6.9 раунда, avg player damage 95.8, dead-end 24/276 rounds
```

Вывод: принимаем как safety valve. Вариант "leap всегда" ухудшал симуляцию, потому что эвристика распыляла доску на лишние острова. Ограничение "только если выбранный тайл заблокирован" сохраняет основной контурный геймплей и дает выход из блокировки.

## Баланс-пасс: честная раздача без guaranteed loop

Гипотеза: если убрать автодостройку полного loop из draw pile, выключить best-of-3 как дефолт и давать обычному игроку новый seed на каждый run, прогон станет менее повторяемым и перестанет системно вести к первому красному квадрату.

Решение для MVP:

- `guaranteedLoopHands = false` в normal config;
- `drawRoundHand` в normal run берет одну руку из draw pile и не заменяет тайлы недостающими corner-loop;
- фиксированный seed вынесен из normal config: обычный старт генерирует новый seed, debug/smoke использует URL `?seed=20260508&guaranteedLoopHands=true`;
- при равных атаках `selectPrimaryAttackColor` возвращает отсутствие primary color, а не красный.

Проверка: seed `20260508`, strict edge matching, 40 честных рук, 40 прогонов каждого боя, 40 случайных попыток выкладки на руку, урон `area * 2`, игрок `120 HP`, текущие HP/атаки из `configs/levels.json`.

```text
honest hands:
placed avg 7.0/7, captures avg 0.1, total damage avg 1.8, zero damage 37/40
red damage avg 0.6, blue damage avg 0.6, green damage avg 0.6

battle_01: 21/40 побед, avg 6.4 раунда, avg player damage 24.9, dead-end 24/256 rounds
battle_02: 23/40 побед, avg 6.5 раунда, avg player damage 40.5, dead-end 29/258 rounds
battle_03: 10/40 побед, avg 7.5 раунда, avg player damage 70.8, dead-end 33/299 rounds
battle_04: 6/40 побед, avg 7.8 раунда, avg player damage 93.1, dead-end 33/310 rounds
battle_05: 6/40 побед, avg 7.7 раунда, avg player damage 112.0, dead-end 31/307 rounds
```

Вывод: повторяемость и красный bias убраны на уровне normal run: seed меняется каждый старт, генератор больше не достраивает готовый loop, а средний урон по red/blue/green в симуляции одинаковый. Цена честности высокая: первый бой просел до 53% побед, а нулевых рук стало 37/40. Это не откатываем в автосборку; следующий проверяемый путь — задачи про queue-режим и color/pattern bag, чтобы добавить разнообразие без скрытой сборки решений за игрока.

## Архив: v2 capture-fill до сглаживания

Симуляция использует `assets/tiles_v2/tile_manifest.json`: на каждый боевой цвет есть `line_h`, `line_v`, четыре `corner`, четыре `tee` и `plus`. Пустая область внутри полностью замкнутой цветной границы считается захваченной землей.

### Строгое совпадение краев

100 случайных рук:

- в среднем ставится 6.9 из 7 тайлов;
- все 7 тайлов поставлены в 88 из 100 рук;
- среднее число захватов: 0.1;
- средний урон: 8.6;
- 94 из 100 рук не нанесли урона;
- максимальный урон руки: 144.

Теоретические бои без апгрейдов:

```text
battle_01: 69/100 побед
battle_02: 61/100 побед
battle_03: 41/100 побед
battle_04: 34/100 побед
battle_05: 17/100 побед
```

Вывод: v2 ожил. Строгое совпадение краев не душит выкладку, и первый бой стал проходимым без апгрейдов. Но урон слишком бинарный: большинство одиночных рук ничего не закрывает, зато редкий захват дает 144 урона и часто решает бой одним взрывом.

### Свободная adjacency-модель

100 случайных рук:

- в среднем ставится 7.0 из 7 тайлов;
- все 7 тайлов поставлены в 100 из 100 рук;
- среднее число захватов: 0.1;
- средний урон: 7.2;
- 95 из 100 рук не нанесли урона;
- максимальный урон руки: 144.

Теоретические бои:

```text
battle_01: 30/100 побед
battle_02: 21/100 побед
battle_03: 11/100 побед
battle_04: 11/100 побед
battle_05:  6/100 побед
```

Вывод: свободная adjacency-модель больше не является автопобедой. В этом эвристическом прогоне она даже слабее строгой модели, потому что случайная выкладка без edge constraints чаще расползается и реже формирует полезные кольца. Для MVP остаемся на строгом edge matching.

## Что показала старая проверка

1. v2-набор `plus/tee/line/corner` работает лучше старого `dot/cap` набора: capture-fill теперь дает победы и большие payoff-моменты.

2. Главная проблема теперь не отсутствие захватов, а волатильность. 94-95% одиночных рук не наносят урон, но редкий захват бьет на 144.

3. Перед боевой сценой нужно сгладить первые минуты:

   - добавить больше маленьких guaranteed/near-guaranteed замыканий;
   - снизить или изменить формулу `площадь * площадь`;
   - ограничить стартовые enemy HP/атаки под rare big hit;
   - проверить стартовую руку/редроу/маленький обучающий capture.

4. `dot` не возвращать в MVP-колоду как обычный тайл. Если нужен маленький стабильный урон, лучше сделать отдельный special-тайл с явным правилом, а не скрытую центральную клетку.

## Архив: строгая модель захвата без пустой внутренности

Эта проверка была промежуточной: она считала только поставленные внутренние micro-cells и не давала очков за пустую внутренность. После обсуждения принято, что замкнутая пустота тоже становится землей. Значит цифры ниже показывают, почему старая осторожная трактовка не подходит, а не финальный баланс.

### Строгое совпадение краев

100 случайных рук:

- в среднем ставится 7.0 из 7 тайлов;
- все 7 тайлов поставлены в 99 из 100 рук;
- среднее число захватов: 0.0;
- средний урон: 0.0;
- 100 из 100 рук не нанесли урона.

Теоретические бои без апгрейдов:

```text
battle_01: 0/100 побед
battle_02: 1/100 побед
battle_03: 0/100 побед
battle_04: 1/100 побед
battle_05: 0/100 побед
```

### Свободная adjacency-модель

100 случайных рук:

- в среднем ставится 7.0 из 7 тайлов;
- все 7 тайлов поставлены в 100 из 100 рук;
- среднее число захватов: 0.0;
- средний урон: 0.0;
- 100 из 100 рук не нанесли урона.

Теоретические бои:

```text
battle_01: 0/100 побед
battle_02: 0/100 побед
battle_03: 0/100 побед
battle_04: 0/100 побед
battle_05: 0/100 побед
```

## Архив: старая компонентная модель

Эти результаты были получены до перехода на правило "граница захватывает землю". Они полезны как проверка выкладываемости и волатильности старой топологии, но больше не являются балансом MVP.

### Строгое совпадение краев

100 случайных рук:

- в среднем ставится 7.0 из 7 тайлов;
- все 7 тайлов поставлены в 99 из 100 рук;
- среднее число закрытых зон: 0.9;
- средний урон: 9.4;
- 35 из 100 рук не нанесли урона;
- максимальный урон руки: 101.

Теоретические бои без апгрейдов:

```text
battle_01: 64/100 побед
battle_02: 40/100 побед
battle_03: 22/100 побед
battle_04:  6/100 побед
battle_05:  2/100 побед
```

Вывод: идея работает, но базовая колода очень волатильная. Это хорошо для риска, но первый бой надо делать мягче, а дальнейшие бои должны предполагать апгрейды. Без апгрейдов 5 боев не должны проходиться стабильно.

### Свободная adjacency-модель

100 случайных рук:

- все руки нанесли урон;
- среднее число закрытых зон: 3.5;
- средний урон: 54.3;
- нулевых рук нет.

Теоретические бои:

```text
battle_01: 100/100 побед
battle_02: 100/100 побед
battle_03: 100/100 побед
battle_04: 100/100 побед
battle_05: 100/100 побед
```

Вывод: если разрешить слишком свободное соседство, риск почти исчезает. Игрок регулярно получает много закрытых зон, и бой превращается в автопобеду.

## Архив: старое решение для MVP

1. Делать MVP со строгой совместимостью краев, но не с одноцветными квадратами.

   Нужны тайлы с внутренней 3x3 или похожей структурой: линии, концевики, углы, острова. Иначе закрытые зоны либо невозможны, либо слишком случайны.

2. Не начинать с атак вроде `красный 5, синий 3, зеленый 7`.

   Для стартовой колоды первые угрозы должны быть в диапазоне 1-3. Числа 5-8 — это уже зона после апгрейдов или поздние бои.

3. Оставить 7 тайлов в руке для первого прототипа.

   По симуляции рука почти всегда выкладывается полностью, значит edge-matching не душит саму выкладку.

4. Добавить безопасные маленькие закрытия.

   `dot`-тайлы дают игроку минимальный урон и понятный пример закрытой зоны. Без таких тайлов будет слишком много пустых раундов.

5. Принять волатильность как часть дизайна, но сгладить первые 3 минуты.

   35% нулевых рук для первого боя многовато. Возможные меры:

   - увеличить долю маленьких островов;
   - дать первый бой с низкими атаками;
   - дать игроку одну гарантированную закрытую зону в стартовой руке;
   - разрешить один бесплатный redraw руки;
   - позже добавить артефакты/апгрейды, которые снижают риск.

6. Считать свободную adjacency-модель запасным вариантом, а не основой.

   Она полезна как emergency fallback, если строгая топология окажется слишком сложной для UX, но в текущем виде она убивает напряжение.

## Что делать перед первой боевой задачей

Реализовывать боевую сцену надо не вокруг абстрактных цветных квадратов, а вокруг конкретного формата тайла:

```text
tile = 3x3 micro-land matrix
edge signature = 3 colors per side
capture boundary = micro-cells одного боевого цвета
captured interior = пустые или поставленные micro-cells, отрезанные boundary от внешнего воздуха
damage = (boundary cells + captured interior cells) * 2
```

Актуальная проверка показывает, что формат micro-grid и v2-набор тайлов годятся для MVP при строгом edge matching, линейном уроне `area * 2` и сглаживании руки через лучшую из 3 кандидатных раздач.
